[skill]
name = "model-evaluator"
version = "0.1.0"
author = "jesaikailash"
description = "Monitor LLM outputs in production. Score quality, detect drift, and alert on degradation across any channel."
category = "agents"
tags = ["Community"]
license = "MIT"
permissions = ["file_read", "file_write", "web_fetch"]
